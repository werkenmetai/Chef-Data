name: AI Support Agent

on:
  issues:
    types: [opened, reopened]
  issue_comment:
    types: [created]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  support:
    # Only run for issues labeled with 'support'
    if: contains(github.event.issue.labels.*.name, 'support')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v2
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build AI agents package
        run: pnpm --filter @exact-mcp/ai-agents build

      # TODO: Implement support agent runner
      # - name: Run Support Agent
      #   env:
      #     ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #     LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
      #   run: |
      #     pnpm --filter @exact-mcp/ai-agents support-agent \
      #       --issue-number=${{ github.event.issue.number }} \
      #       --event-type=${{ github.event_name }}

      - name: Placeholder response
        run: |
          echo "Support agent triggered for issue #${{ github.event.issue.number }}"
          echo "Event type: ${{ github.event_name }}"
          echo "Note: Support agent not yet fully implemented"
