{
  "name": "@exact-mcp/ai-agents",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent definitions for Exact Online MCP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@exact-mcp/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
